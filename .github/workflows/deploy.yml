# Phase 4 recovery republish: 2026-07-25
# CIPMN secure import console republish: 2026-07-29
# CIPMN Google Drive video player production release: 2026-07-30
# Materials search, tags and centred player release: 2026-07-30
# CIPMN Access My Materials email deep-link release: 2026-07-31
# CIPMN examination-card startup hotfix release: 2026-07-31
# Locked CIPMN payment and cart controls release: 2026-07-31
# Final visible CIPMN cart and payment actions release: 2026-07-31
# Approved IIPM and CIPMN certificate template release: 2026-07-31
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/deploy.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Supabase integration branch
        uses: actions/checkout@v6
        with:
          ref: supabase-integration

      - name: Set up Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Validate TypeScript
        run: npm run lint

      - name: Build GitHub Pages portal
        run: npx vite build

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
