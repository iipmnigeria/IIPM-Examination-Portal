name: Temporary PR 312 Catalogue Validation

on:
  pull_request:
    branches:
      - supabase-integration
    paths:
      - "src/components/AgileProgrammeCatalogue.tsx"
      - "src/data/agileProgrammeCatalogue.ts"
      - "src/main.tsx"
      - "docs/AGILECERT_PROGRAMME_CATALOGUE_FOUNDATION.md"
      - ".github/workflows/validate-pr-312-temporary.yml"

permissions:
  contents: read

jobs:
  validate:
    name: Scoped typecheck and production build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out pull request
        uses: actions/checkout@v6

      - name: Set up Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Create scoped catalogue TypeScript project
        run: |
          cat > tsconfig.catalogue.json <<'JSON'
          {
            "extends": "./tsconfig.json",
            "compilerOptions": {
              "noEmit": true
            },
            "include": [
              "src/components/AgileProgrammeCatalogue.tsx",
              "src/data/agileProgrammeCatalogue.ts"
            ],
            "exclude": [
              "scripts/**",
              "supabase/functions/**"
            ]
          }
          JSON

      - name: Run scoped TypeScript validation
        run: npx tsc -p tsconfig.catalogue.json

      - name: Build production application
        run: npm run build
