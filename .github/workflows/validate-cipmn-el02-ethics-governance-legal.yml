name: Validate CIPMN EL02 Ethics Governance Legal

on:
  push:
    branches:
      - safe/cipmn-el02-ethics-governance-legal
    paths:
      - 'supabase/migrations/20260926103000_cipmn_el02_ethics_governance_legal.sql'
      - 'scripts/validate-cipmn-el02-ethics-governance-legal.mjs'
      - '.github/workflows/validate-cipmn-el02-ethics-governance-legal.yml'
  pull_request:
    branches: [supabase-integration]
    paths:
      - 'supabase/migrations/20260926103000_cipmn_el02_ethics_governance_legal.sql'
      - 'scripts/validate-cipmn-el02-ethics-governance-legal.mjs'
      - '.github/workflows/validate-cipmn-el02-ethics-governance-legal.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-cipmn-el02-ethics-governance-legal-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate EL02 source-strict elective
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout candidate changes
        uses: actions/checkout@v4

      - name: Set up Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Validate EL02 source scope, structure and migration safety
        run: node scripts/validate-cipmn-el02-ethics-governance-legal.mjs

      - name: Install application dependencies
        run: npm install --no-audit --no-fund

      - name: Existing TypeScript validation
        run: npm run lint

      - name: Production build
        run: npm run build

      - name: Verify production artifact
        shell: bash
        run: |
          set -euo pipefail
          test -s dist/index.html
          grep -q 'AgileCert Global' dist/index.html
