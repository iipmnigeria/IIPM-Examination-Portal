name: Publish Phase 3 to Main Pages Source

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - 'deployment-triggers/publish-phase-3-main-pages.txt'

permissions:
  contents: write

concurrency:
  group: publish-phase-3-main-pages
  cancel-in-progress: false

jobs:
  publish-and-verify:
    name: Publish compiled Phase 3 bundle and verify live portal
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      APPROVED_APP_COMMIT: 'dc98a784029ab1d08fe6e6a672983d169aece476'
      LIVE_URL: https://iipmnigeria.github.io/IIPM-Examination-Portal/

    steps:
      - name: Checkout exact approved production application
        uses: actions/checkout@v4
        with:
          ref: ${{ env.APPROVED_APP_COMMIT }}
          fetch-depth: 0

      - name: Confirm approved commit and Phase 3 source
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$APPROVED_APP_COMMIT"
          grep -q 'Certificate Authority Workspace' src/components/CandidateCertificateWorkspace.tsx
          grep -q 'Verify Certificate' src/components/PublicCertificateVerification.tsx
          grep -q 'Certificate Authority' src/components/AdminCertificateManagementLauncher.tsx
          grep -q 'Server issuance required' src/components/LegacyCertificateSecurityGate.tsx
          grep -q 'verify_agilecert_certificate' src/services/certificateService.ts
          grep -q 'request_my_agilecert_certificate' src/services/certificateService.ts
          grep -q 'issue_agilecert_certificate' src/services/certificateService.ts
          grep -q 'PublicCertificateVerification' src/main.tsx
          grep -q 'LegacyCertificateSecurityGate' src/main.tsx

      - name: Set up Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install and build
        shell: bash
        run: |
          set -euo pipefail
          npm install
          npm run build

      - name: Verify and fingerprint compiled bundle
        shell: bash
        run: |
          set -euo pipefail
          test -s dist/index.html
          grep -q 'AgileCert Global' dist/index.html
          if grep -q '/src/main.tsx' dist/index.html; then
            echo 'Compiled output incorrectly references the Vite source entry.'
            exit 1
          fi
          asset="$(grep -Eo 'src="[^"]+\.js"' dist/index.html | head -n 1 | cut -d'"' -f2)"
          test -n "$asset"
          js_file="dist/${asset#./}"
          test -s "$js_file"
          test "$(wc -c < "$js_file")" -gt 1000
          grep -q 'Verify Certificate' "$js_file"
          grep -q 'IIPM Certificate Verification' "$js_file"
          sha256sum "$js_file" | awk '{print $1}' | tee "$RUNNER_TEMP/expected-live-js.sha256"
          rm -f dist/server.cjs dist/server.cjs.map
          touch dist/.nojekyll

      - name: Overlay compiled deployment files on main
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          rm -rf /tmp/agilecert-main-pages
          git worktree add --detach /tmp/agilecert-main-pages origin/main

          rm -rf /tmp/agilecert-main-pages/assets
          rm -f /tmp/agilecert-main-pages/server.cjs /tmp/agilecert-main-pages/server.cjs.map
          cp -a dist/. /tmp/agilecert-main-pages/

          cd /tmp/agilecert-main-pages
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A -- index.html assets .nojekyll server.cjs server.cjs.map 2>/dev/null || true
          while IFS= read -r file; do
            case "$file" in
              index.html|.nojekyll) ;;
              *) git add -A -- "$file" ;;
            esac
          done < <(find "$GITHUB_WORKSPACE/dist" -maxdepth 1 -type f -printf '%f\n')

          if git diff --cached --quiet; then
            echo 'The Pages source already contains the approved compiled bundle.'
          else
            git commit -m "Deploy Phase 3 ${APPROVED_APP_COMMIT}"
            git push origin HEAD:main
          fi

      - name: Wait for exact compiled live bundle
        shell: bash
        run: |
          set -euo pipefail
          expected_sha="$(cat "$RUNNER_TEMP/expected-live-js.sha256")"
          success=0
          for attempt in $(seq 1 36); do
            status="$(curl -L -sS -o "$RUNNER_TEMP/live-index.html" -w '%{http_code}' "$LIVE_URL?phase3-main=${GITHUB_RUN_ID}-${attempt}" || true)"
            echo "Attempt $attempt: HTTP $status"
            if [ "$status" = '200' ] \
              && grep -q 'AgileCert Global' "$RUNNER_TEMP/live-index.html" \
              && ! grep -q '/src/main.tsx' "$RUNNER_TEMP/live-index.html"; then
              asset="$(grep -Eo 'src="[^"]+\.js"' "$RUNNER_TEMP/live-index.html" | head -n 1 | cut -d'"' -f2)"
              if [ -n "$asset" ]; then
                asset_url="$(python3 - "$LIVE_URL" "$asset" <<'PY'
          from urllib.parse import urljoin
          import sys
          print(urljoin(sys.argv[1], sys.argv[2]))
          PY
                )"
                if curl -L --fail --silent --show-error "$asset_url?phase3=${GITHUB_RUN_ID}-${attempt}" -o "$RUNNER_TEMP/live-app.js"; then
                  live_sha="$(sha256sum "$RUNNER_TEMP/live-app.js" | awk '{print $1}')"
                  echo "Expected JS SHA-256: $expected_sha"
                  echo "Live JS SHA-256:     $live_sha"
                  if [ "$live_sha" = "$expected_sha" ]; then
                    success=1
                    break
                  fi
                fi
              fi
            fi
            sleep 10
          done
          test "$success" -eq 1

      - name: Install Chromium
        shell: bash
        run: |
          mkdir -p "$RUNNER_TEMP/live-smoke"
          cd "$RUNNER_TEMP/live-smoke"
          npm init -y >/dev/null
          npm install --no-audit --no-fund playwright@latest
          npx playwright install --with-deps chromium

      - name: Render and inspect deployed portal and verification service
        shell: bash
        working-directory: ${{ runner.temp }}/live-smoke
        run: |
          node <<'NODE'
          const fs = require('fs');
          const { chromium } = require('playwright');

          (async () => {
            const browser = await chromium.launch({ headless: true });
            const page = await browser.newPage({ viewport: { width: 1440, height: 1000 } });
            const pageErrors = [];
            const consoleErrors = [];
            page.on('pageerror', error => pageErrors.push(String(error)));
            page.on('console', message => {
              if (message.type() === 'error') consoleErrors.push(message.text());
            });

            const response = await page.goto(`${process.env.LIVE_URL}?phase3-main-final=${process.env.GITHUB_RUN_ID}`, {
              waitUntil: 'domcontentloaded',
              timeout: 60000,
            });
            await page.waitForTimeout(6000);

            const status = response ? response.status() : 0;
            const title = await page.title();
            const bodyText = (await page.locator('body').innerText()).trim();
            const root = page.locator('#root');
            const rootCount = await root.count();
            const rootHtml = rootCount ? (await root.innerHTML()).trim() : '';
            const bootShellVisible = await page.locator('#iipm-boot-shell').isVisible().catch(() => false);

            const verifyLauncher = page.getByRole('button', { name: 'Verify an IIPM certificate' });
            await verifyLauncher.waitFor({ state: 'visible', timeout: 15000 });
            await verifyLauncher.click();
            await page.getByRole('heading', { name: 'IIPM Certificate Verification' }).waitFor({ state: 'visible' });
            await page.locator('#certificate-verification-code').fill('AGILECERT-NOT-A-REAL-CERTIFICATE-000000');
            await page.getByRole('button', { name: 'Verify', exact: true }).click();
            await page.getByRole('heading', { name: 'Certificate not verified' }).waitFor({ state: 'visible', timeout: 20000 });

            const privacyNoticeVisible = await page.getByText(/does not expose the candidate.s account ID/i).isVisible();
            const verificationResultVisible = await page.getByRole('heading', { name: 'Certificate not verified' }).isVisible();

            const report = {
              url: page.url(),
              status,
              title,
              bodyTextLength: bodyText.length,
              rootHtmlLength: rootHtml.length,
              bootShellVisible,
              verificationLauncherVisible: true,
              verificationResultVisible,
              privacyNoticeVisible,
              pageErrors,
              consoleErrors,
              bodyPreview: bodyText.slice(0, 700),
            };

            fs.writeFileSync(`${process.env.RUNNER_TEMP}/live-phase-3-main-report.json`, JSON.stringify(report, null, 2));
            await page.screenshot({ path: `${process.env.RUNNER_TEMP}/live-phase-3-main.png`, fullPage: true });
            await browser.close();

            if (status !== 200) throw new Error(`Live portal returned HTTP ${status}.`);
            if (!/AgileCert Global/i.test(title)) throw new Error(`Unexpected page title: ${title}`);
            if (rootCount !== 1 || rootHtml.length < 100) throw new Error('The application root is blank or missing.');
            if (bodyText.length < 100) throw new Error('The rendered page has insufficient visible content.');
            if (bootShellVisible) throw new Error('The static loading shell did not hand over to React.');
            if (!verificationResultVisible || !privacyNoticeVisible) throw new Error('The public certificate verification boundary did not render correctly.');
            if (pageErrors.length) throw new Error(`Browser page errors: ${pageErrors.join(' | ')}`);
            if (consoleErrors.length) throw new Error(`Browser console errors: ${consoleErrors.join(' | ')}`);
          })().catch(error => {
            console.error(error);
            process.exit(1);
          });
          NODE

      - name: Upload final live evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: live-agilecert-phase-3-main-evidence
          path: |
            ${{ runner.temp }}/expected-live-js.sha256
            ${{ runner.temp }}/live-index.html
            ${{ runner.temp }}/live-app.js
            ${{ runner.temp }}/live-phase-3-main-report.json
            ${{ runner.temp }}/live-phase-3-main.png
          if-no-files-found: warn
          retention-days: 3
