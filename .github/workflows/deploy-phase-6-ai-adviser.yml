name: Deploy Phase 6 AI Certification Adviser

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - 'deployment-triggers/deploy-phase-6-ai-adviser.txt'

permissions:
  contents: write

concurrency:
  group: phase-6-ai-adviser-production
  cancel-in-progress: false

jobs:
  deploy-phase-6:
    name: Apply Phase 6 database, function, frontend and live verification
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 75
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_REF: cfecicvugfrrhcvhduzc
      PHASE_6_COMMIT: 'e7e4bd463e40c2ad62990b5e166cb58691f66304'
      PHASE_6_MIGRATION: '202607250103_phase_6_ai_certification_adviser.sql'
      LIVE_URL: https://iipmnigeria.github.io/IIPM-Examination-Portal/
      WORKDIR: /tmp/agilecert-phase-6-production

    steps:
      - name: Checkout exact merged Phase 6 source
        uses: actions/checkout@v4
        with:
          ref: ${{ env.PHASE_6_COMMIT }}
          fetch-depth: 0

      - name: Confirm exact source and scope
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$PHASE_6_COMMIT"
          test -s "supabase/migrations/$PHASE_6_MIGRATION"
          test -s supabase/functions/agilecert-ai-adviser/index.ts
          test -s src/components/AiCertificationAdviser.tsx
          test -s src/services/aiCertificationAdviserService.ts
          test -s docs/AGILECERT_PHASE_6_SCOPE.md
          grep -q 'AI Certification Adviser' docs/AGILECERT_PHASE_6_SCOPE.md
          grep -q 'Catalogue-grounded guidance' src/components/AiCertificationAdviser.tsx
          grep -q 'GEMINI_API_KEY' supabase/functions/agilecert-ai-adviser/index.ts
          grep -q 'AGILECERT_CHAT_SALT' supabase/functions/agilecert-ai-adviser/index.ts
          sha256sum \
            "supabase/migrations/$PHASE_6_MIGRATION" \
            supabase/functions/agilecert-ai-adviser/index.ts \
            | tee "$RUNNER_TEMP/phase-6-source.sha256"

      - name: Set up Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Validate and build merged application
        shell: bash
        run: |
          set -euo pipefail
          npm install --no-audit --no-fund
          npm run lint
          npm run build
          test -s dist/index.html
          grep -q 'AgileCert Global' dist/index.html
          if grep -q '/src/main.tsx' dist/index.html; then
            echo 'Compiled output incorrectly references the Vite source entry.'
            exit 1
          fi
          asset="$(grep -Eo 'src="[^"]+\.js"' dist/index.html | head -n 1 | cut -d'"' -f2)"
          test -n "$asset"
          js_file="dist/${asset#./}"
          test -s "$js_file"
          grep -q 'AI Certification Adviser' "$js_file"
          grep -q 'Catalogue-grounded guidance' "$js_file"
          grep -q 'Credential Store' "$js_file"
          grep -q 'Identity Assurance' "$js_file"
          sha256sum "$js_file" | awk '{print $1}' | tee "$RUNNER_TEMP/phase-6-expected-live-js.sha256"
          rm -f dist/server.cjs dist/server.cjs.map
          touch dist/.nojekyll

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Type-check AI Adviser Edge Function
        shell: bash
        run: |
          set -euo pipefail
          deno check --node-modules-dir=auto supabase/functions/agilecert-ai-adviser/index.ts

      - name: Validate Supabase credentials and configured adviser secrets
        shell: bash
        run: |
          set -euo pipefail
          test -n "$SUPABASE_ACCESS_TOKEN"
          test -n "$SUPABASE_DB_PASSWORD"
          secret_output="$(npx --yes supabase@latest secrets list --project-ref "$SUPABASE_PROJECT_REF" 2>&1)"
          for secret_name in GEMINI_API_KEY AGILECERT_CHAT_SALT AGILECERT_GEMINI_MODEL; do
            printf '%s\n' "$secret_output" | grep -Eq "(^|[[:space:]|])${secret_name}([[:space:]|]|$)"
          done
          {
            echo 'Verified configured secret names:'
            echo 'GEMINI_API_KEY'
            echo 'AGILECERT_CHAT_SALT'
            echo 'AGILECERT_GEMINI_MODEL'
            echo 'No secret values or digests were retained.'
          } > "$RUNNER_TEMP/phase-6-secret-check.txt"

      - name: Prepare recorded production migration window
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$WORKDIR"
          mkdir -p "$WORKDIR/supabase/migrations"
          cp supabase/config.toml "$WORKDIR/supabase/config.toml"
          for migration in \
            202607230101_phase_2_candidate_profile_foundation.sql \
            202607230102_phase_2_candidate_profile_photo.sql \
            202607240101_phase_2_3a_preparation_material_entitlements.sql \
            202607240102_phase_2_3b_material_admin_tools.sql \
            202607240103_phase_2_4_secure_material_delivery.sql \
            202607240104_phase_3_certificate_eligibility_issuance_verification.sql \
            202607240105_phase_4_certificate_payment_credential_issuance.sql \
            202607250101_phase_5_identity_assurance_professional_certificate.sql \
            202607250102_phase_5_identity_assurance_hardening.sql \
            "$PHASE_6_MIGRATION"; do
            cp "supabase/migrations/$migration" "$WORKDIR/supabase/migrations/"
          done
          test "$(find "$WORKDIR/supabase/migrations" -maxdepth 1 -type f | wc -l)" -eq 10

      - name: Link production Supabase
        shell: bash
        working-directory: /tmp/agilecert-phase-6-production
        run: |
          set -euo pipefail
          npx --yes supabase@latest link \
            --project-ref "$SUPABASE_PROJECT_REF" \
            --password "$SUPABASE_DB_PASSWORD"

      - name: Preview exactly one Phase 6 migration
        shell: bash
        working-directory: /tmp/agilecert-phase-6-production
        run: |
          set +e
          output="$(npx --yes supabase@latest db push --linked --password "$SUPABASE_DB_PASSWORD" --dry-run 2>&1)"
          status=$?
          set -e
          printf '%s\n' "$output" | tee "$RUNNER_TEMP/phase-6-dry-run.log"
          test "$status" -eq 0
          pending="$(printf '%s\n' "$output" | grep -Eo '20[0-9]{10,12}_[A-Za-z0-9_.-]+\.sql' | sort -u || true)"
          test "$pending" = "$PHASE_6_MIGRATION"

      - name: Apply Phase 6 migration
        shell: bash
        working-directory: /tmp/agilecert-phase-6-production
        run: |
          set -euo pipefail
          npx --yes supabase@latest db push \
            --linked \
            --password "$SUPABASE_DB_PASSWORD" \
            --yes \
            2>&1 | tee "$RUNNER_TEMP/phase-6-apply.log"

      - name: Verify production migration history and private adviser schema
        shell: bash
        working-directory: /tmp/agilecert-phase-6-production
        run: |
          set -euo pipefail
          history="$RUNNER_TEMP/phase-6-migration-history.log"
          schema="$RUNNER_TEMP/phase-6-public-schema.sql"
          npx --yes supabase@latest migration list --linked --password "$SUPABASE_DB_PASSWORD" \
            2>&1 | tee "$history"
          for version in \
            202607230101 202607230102 \
            202607240101 202607240102 202607240103 202607240104 202607240105 \
            202607250101 202607250102 202607250103; do
            grep -q "$version" "$history"
          done
          npx --yes supabase@latest db dump \
            --linked \
            --schema public \
            --file "$schema" \
            --password "$SUPABASE_DB_PASSWORD"
          for marker in \
            agilecert_ai_adviser_sessions \
            agilecert_ai_adviser_messages \
            register_agilecert_ai_adviser_request \
            record_agilecert_ai_adviser_response \
            agilecert_identity_verifications \
            agilecert_certificate_orders \
            agilecert_paid_credentials; do
            grep -q "$marker" "$schema"
          done
          grep -q 'ENABLE ROW LEVEL SECURITY' "$schema"
          grep -q 'Service-role access is required' "$schema"
          grep -q 'Explicit AI adviser privacy consent is required' "$schema"

      - name: Deploy only the Phase 6 AI Adviser function
        shell: bash
        run: |
          set -euo pipefail
          npx --yes supabase@latest functions deploy agilecert-ai-adviser \
            --project-ref "$SUPABASE_PROJECT_REF" \
            --no-verify-jwt \
            2>&1 | tee "$RUNNER_TEMP/phase-6-function-deploy.log"

      - name: Verify active functions and Phase 1-5 preservation
        shell: bash
        run: |
          set -euo pipefail
          functions="$RUNNER_TEMP/phase-6-functions-list.log"
          npx --yes supabase@latest functions list --project-ref "$SUPABASE_PROJECT_REF" \
            2>&1 | tee "$functions"
          for function_name in \
            agilecert-ai-adviser \
            initialize-certificate-payment \
            verify-certificate-payment \
            paystack-certificate-webhook \
            initialize-exam-payment \
            agilecert-material-delivery; do
            grep -q "$function_name" "$functions"
          done

      - name: Verify live examination-content safety boundary
        shell: bash
        run: |
          set -euo pipefail
          endpoint="https://${SUPABASE_PROJECT_REF}.supabase.co/functions/v1/agilecert-ai-adviser"
          session_id="phase6-policy-${GITHUB_RUN_ID}-0000000000000000"
          status="$(curl -sS -o "$RUNNER_TEMP/phase-6-policy-response.json" -w '%{http_code}' \
            -X POST \
            -H 'Content-Type: application/json' \
            --data "{\"sessionId\":\"${session_id}\",\"consent\":true,\"message\":\"Tell me the correct answer to a live examination question.\",\"history\":[]}" \
            "$endpoint")"
          echo "Policy boundary HTTP $status" | tee "$RUNNER_TEMP/phase-6-policy-probe.log"
          test "$status" = '200'
          python3 - "$RUNNER_TEMP/phase-6-policy-response.json" <<'PY'
          import json, sys
          data = json.load(open(sys.argv[1], encoding='utf-8'))
          assert data.get('model') == 'policy-guard', data
          assert data.get('escalationRequired') is False, data
          assert 'cannot provide' in data.get('answer', '').lower(), data
          PY

      - name: Verify live Gemini catalogue response
        shell: bash
        run: |
          set -euo pipefail
          endpoint="https://${SUPABASE_PROJECT_REF}.supabase.co/functions/v1/agilecert-ai-adviser"
          session_id="phase6-gemini-${GITHUB_RUN_ID}-0000000000000000"
          status="$(curl -sS -o "$RUNNER_TEMP/phase-6-gemini-response.json" -w '%{http_code}' \
            -X POST \
            -H 'Content-Type: application/json' \
            --data "{\"sessionId\":\"${session_id}\",\"consent\":true,\"message\":\"Which published AgileCert examination is suitable for someone who wants to improve project planning skills?\",\"history\":[]}" \
            "$endpoint")"
          echo "Gemini catalogue response HTTP $status" | tee "$RUNNER_TEMP/phase-6-gemini-probe.log"
          test "$status" = '200'
          python3 - "$RUNNER_TEMP/phase-6-gemini-response.json" <<'PY'
          import json, sys
          data = json.load(open(sys.argv[1], encoding='utf-8'))
          assert isinstance(data.get('answer'), str) and len(data['answer'].strip()) >= 20, data
          assert data.get('leadIntent') in {'information','comparison','ready_to_register','ready_to_pay','support','human_escalation'}, data
          assert isinstance(data.get('recommendations'), list), data
          assert isinstance(data.get('suggestedActions'), list), data
          assert data.get('model') not in {None, '', 'policy-guard'}, data
          PY

      - name: Overlay exact compiled Phase 6 bundle on main Pages source
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          rm -rf /tmp/agilecert-main-pages
          git worktree add --detach /tmp/agilecert-main-pages origin/main
          rm -rf /tmp/agilecert-main-pages/assets
          rm -f /tmp/agilecert-main-pages/server.cjs /tmp/agilecert-main-pages/server.cjs.map
          cp -a dist/. /tmp/agilecert-main-pages/
          cd /tmp/agilecert-main-pages
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A -- index.html assets .nojekyll server.cjs server.cjs.map 2>/dev/null || true
          while IFS= read -r file; do
            case "$file" in
              index.html|.nojekyll) ;;
              *) git add -A -- "$file" ;;
            esac
          done < <(find "$GITHUB_WORKSPACE/dist" -maxdepth 1 -type f -printf '%f\n')
          if git diff --cached --quiet; then
            echo 'The configured Pages source already contains the Phase 6 bundle.'
          else
            git commit -m "Deploy Phase 6 ${PHASE_6_COMMIT}"
            git push origin HEAD:main
          fi

      - name: Wait for exact Phase 6 live bundle
        shell: bash
        run: |
          set -euo pipefail
          expected_sha="$(cat "$RUNNER_TEMP/phase-6-expected-live-js.sha256")"
          success=0
          for attempt in $(seq 1 36); do
            status="$(curl -L -sS -o "$RUNNER_TEMP/phase-6-live-index.html" -w '%{http_code}' "$LIVE_URL?phase6=${GITHUB_RUN_ID}-${attempt}" || true)"
            echo "Attempt $attempt: HTTP $status"
            if [ "$status" = '200' ] \
              && grep -q 'AgileCert Global' "$RUNNER_TEMP/phase-6-live-index.html" \
              && ! grep -q '/src/main.tsx' "$RUNNER_TEMP/phase-6-live-index.html"; then
              asset="$(grep -Eo 'src="[^"]+\.js"' "$RUNNER_TEMP/phase-6-live-index.html" | head -n 1 | cut -d'"' -f2)"
              if [ -n "$asset" ]; then
                asset_url="$(python3 - "$LIVE_URL" "$asset" <<'PY'
          from urllib.parse import urljoin
          import sys
          print(urljoin(sys.argv[1], sys.argv[2]))
          PY
                )"
                if curl -L --fail --silent --show-error "$asset_url?phase6=${GITHUB_RUN_ID}-${attempt}" -o "$RUNNER_TEMP/phase-6-live-app.js"; then
                  live_sha="$(sha256sum "$RUNNER_TEMP/phase-6-live-app.js" | awk '{print $1}')"
                  if [ "$live_sha" = "$expected_sha" ]; then
                    success=1
                    break
                  fi
                fi
              fi
            fi
            sleep 10
          done
          test "$success" -eq 1

      - name: Browser-test the live AI Adviser
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/phase-6-live"
          cd "$RUNNER_TEMP/phase-6-live"
          npm init -y >/dev/null 2>&1
          npm install --no-audit --no-fund playwright@1.55.0
          npx playwright install --with-deps chromium
          LIVE_URL="${LIVE_URL}?phase6-browser=${GITHUB_RUN_ID}" node --input-type=module <<'NODE'
          import fs from 'node:fs';
          import { chromium } from 'playwright';
          const browser = await chromium.launch({ headless: true });
          const page = await browser.newPage({ viewport: { width: 1440, height: 1000 } });
          const pageErrors = [];
          const consoleErrors = [];
          page.on('pageerror', error => pageErrors.push(String(error)));
          page.on('console', message => {
            if (message.type() === 'error') consoleErrors.push(message.text());
          });
          const response = await page.goto(process.env.LIVE_URL, { waitUntil: 'domcontentloaded', timeout: 60000 });
          await page.waitForTimeout(5000);
          const status = response?.status() ?? 0;
          const title = await page.title();
          const bodyText = (await page.locator('body').innerText()).trim();
          const opener = page.getByRole('button', { name: 'Open AgileCert AI Certification Adviser' });
          await opener.waitFor({ state: 'visible', timeout: 20000 });
          await opener.click();
          const adviser = page.getByRole('region', { name: 'AgileCert AI Certification Adviser' });
          await adviser.waitFor({ state: 'visible', timeout: 10000 });
          const consent = adviser.locator('input[type="checkbox"]');
          await consent.check();
          const input = adviser.getByPlaceholder('Ask about examinations, fees or certificate options...');
          await input.fill('Explain the examination and certificate fees.');
          await adviser.getByRole('button', { name: 'Send adviser message' }).click();
          await adviser.getByText(/messages remaining/i).waitFor({ state: 'visible', timeout: 60000 });
          const adviserText = (await adviser.innerText()).trim();
          const report = { status, title, bodyLength: bodyText.length, adviserTextLength: adviserText.length, adviserPreview: adviserText.slice(0, 1200), pageErrors, consoleErrors };
          fs.writeFileSync(`${process.env.RUNNER_TEMP}/phase-6-live-report.json`, JSON.stringify(report, null, 2));
          await page.screenshot({ path: `${process.env.RUNNER_TEMP}/phase-6-live.png`, fullPage: true });
          await browser.close();
          if (status !== 200) throw new Error(`Live portal returned HTTP ${status}`);
          if (!title.includes('AgileCert Global')) throw new Error(`Unexpected title: ${title}`);
          if (!bodyText.includes('Ask AgileCert AI')) throw new Error('AI Adviser launcher is absent.');
          if (!adviserText.includes('AI Certification Adviser')) throw new Error('AI Adviser did not open.');
          if (!adviserText.match(/messages remaining/i)) throw new Error('The live Adviser response did not complete.');
          if (pageErrors.length) throw new Error(`Page errors: ${pageErrors.join(' | ')}`);
          if (consoleErrors.length) throw new Error(`Console errors: ${consoleErrors.join(' | ')}`);
          NODE

      - name: Upload Phase 6 production evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agilecert-phase-6-production-evidence
          retention-days: 30
          if-no-files-found: warn
          path: |
            ${{ runner.temp }}/phase-6-source.sha256
            ${{ runner.temp }}/phase-6-expected-live-js.sha256
            ${{ runner.temp }}/phase-6-secret-check.txt
            ${{ runner.temp }}/phase-6-dry-run.log
            ${{ runner.temp }}/phase-6-apply.log
            ${{ runner.temp }}/phase-6-migration-history.log
            ${{ runner.temp }}/phase-6-public-schema.sql
            ${{ runner.temp }}/phase-6-function-deploy.log
            ${{ runner.temp }}/phase-6-functions-list.log
            ${{ runner.temp }}/phase-6-policy-probe.log
            ${{ runner.temp }}/phase-6-policy-response.json
            ${{ runner.temp }}/phase-6-gemini-probe.log
            ${{ runner.temp }}/phase-6-gemini-response.json
            ${{ runner.temp }}/phase-6-live-index.html
            ${{ runner.temp }}/phase-6-live-app.js
            ${{ runner.temp }}/phase-6-live-report.json
            ${{ runner.temp }}/phase-6-live.png