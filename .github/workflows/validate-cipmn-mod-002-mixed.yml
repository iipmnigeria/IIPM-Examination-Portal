name: Validate CIPMN Module 2 Mixed Conversion

on:
  push:
    branches:
      - safe/cipmn-mod-002-mixed-exam
    paths:
      - 'supabase/migrations/20260925214500_cipmn_mod_002_mixed_exam.sql'
      - 'scripts/validate-cipmn-mod-002-mixed.mjs'
      - '.github/workflows/validate-cipmn-mod-002-mixed.yml'
  pull_request:
    branches: [supabase-integration]
    paths:
      - 'supabase/migrations/20260925214500_cipmn_mod_002_mixed_exam.sql'
      - 'scripts/validate-cipmn-mod-002-mixed.mjs'
      - '.github/workflows/validate-cipmn-mod-002-mixed.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-cipmn-mod-002-mixed-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Module 2 mixed-exam migration
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout candidate changes
        uses: actions/checkout@v4

      - name: Set up Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Validate migration safety contracts
        run: node scripts/validate-cipmn-mod-002-mixed.mjs

      - name: Install application dependencies
        run: npm install --no-audit --no-fund

      - name: Existing TypeScript validation
        run: npm run lint

      - name: Production build
        run: npm run build

      - name: Verify production artifact
        shell: bash
        run: |
          set -euo pipefail
          test -s dist/index.html
          grep -q 'AgileCert Global' dist/index.html
